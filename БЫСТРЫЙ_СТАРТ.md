# Быстрый Старт - MVP Сервис Уведомлений

## Что было сделано

✅ **Проект полностью переработан в универсальный MVP**

### Выполненные задачи

1. ✅ Удалены все зависимости от Docsvision
2. ✅ Созданы три универсальных обработчика уведомлений
3. ✅ Добавлена поддержка SignalR для real-time уведомлений
4. ✅ Создано тестовое приложение
5. ✅ Написана полная документация

## Запуск проекта

### Самый простой способ (рекомендуется)

```bash
cd testapp
./start.sh      # Linux/Mac
# или
start.bat       # Windows
```

После запуска откройте http://localhost:8080 в браузере.

### Ручной запуск

#### Backend API

```bash
cd backend
dotnet build
dotnet run --project src/NotificationService.Api
```

API будет доступен по адресу:
- http://localhost:5000/api - REST API
- http://localhost:5000/swagger - Swagger UI
- http://localhost:5000/notificationHub - SignalR Hub

#### Frontend компонент (опционально)

```bash
cd frontend/sed-notifications-frontend
npm install
npm start
```

Откроется на http://localhost:3000

## Созданные обработчики уведомлений

### 1. UserRegistered (Регистрация пользователя)
Отправляет приветственное уведомление новому пользователю.

**Пример запроса:**
```json
POST http://localhost:5000/api/notification
Content-Type: application/json

{
  "route": "UserRegistered",
  "channel": "Email",
  "parameters": {
    "UserId": "00000000-0000-0000-0000-000000000001",
    "WelcomeMessage": "Добро пожаловать!"
  }
}
```

### 2. OrderCreated (Создание заказа)
Отправляет подтверждение заказа.

**Пример запроса:**
```json
POST http://localhost:5000/api/notification
Content-Type: application/json

{
  "route": "OrderCreated",
  "channel": "Email",
  "parameters": {
    "CustomerId": "00000000-0000-0000-0000-000000000001",
    "OrderNumber": "ORD-12345",
    "OrderTotal": 2999.99,
    "ItemCount": 5
  }
}
```

### 3. TaskAssigned (Назначение задачи)
Отправляет уведомление о назначении задачи.

**Пример запроса:**
```json
POST http://localhost:5000/api/notification
Content-Type: application/json

{
  "route": "TaskAssigned",
  "channel": "Email",
  "parameters": {
    "AssigneeId": "00000000-0000-0000-0000-000000000001",
    "AssignerId": "00000000-0000-0000-0000-000000000002",
    "TaskTitle": "Завершить MVP",
    "TaskDescription": "Создать полноценный MVP сервиса уведомлений",
    "Priority": "High",
    "DueDate": "2025-12-31T23:59:59Z"
  }
}
```

## SignalR Real-Time Уведомления

### Подключение к SignalR Hub

```javascript
const connection = new signalR.HubConnectionBuilder()
    .withUrl("http://localhost:5000/notificationHub")
    .withAutomaticReconnect()
    .build();

connection.on("ReceiveNotification", (notification) => {
    console.log("Получено уведомление:", notification);
    // Обработка уведомления в UI
});

await connection.start();
```

### Тестовая трансляция

```bash
POST http://localhost:5000/api/notification/broadcast
Content-Type: application/json

{
  "title": "Тестовое уведомление",
  "message": "Это тестовое уведомление для всех подключенных клиентов",
  "route": "Test"
}
```

## Тестовое приложение

Тестовое приложение находится в папке `testapp/` и предоставляет:

- ✅ Красивый веб-интерфейс
- ✅ Кнопки для отправки всех типов уведомлений
- ✅ Real-time отображение уведомлений через SignalR
- ✅ Индикатор подключения
- ✅ Историю полученных уведомлений

## Структура проекта

```
notifications-service/
├── backend/                          # Backend .NET сервис
│   ├── src/
│   │   ├── NotificationService.Api/       # REST API + SignalR
│   │   ├── NotificationService.TestHandlers/  # Обработчики (НОВОЕ)
│   │   ├── NotificationService.Application/
│   │   ├── NotificationService.Domain/
│   │   └── NotificationService.Infrastructure/
├── frontend/                         # Frontend React компонент
│   └── sed-notifications-frontend/
├── testapp/                          # Тестовое приложение (НОВОЕ)
│   ├── index.html
│   ├── start.sh
│   └── start.bat
└── README.md                         # Полная документация
```

## Добавление своих обработчиков

Чтобы добавить новый тип уведомлений:

1. Создайте папку в `NotificationService.TestHandlers/Notifications/`
2. Реализуйте `INotificationDataResolver`
3. Реализуйте `INotificationRouteConfiguration`
4. Создайте HTML шаблон (`.hbs` файл)
5. Создайте конфигурацию шаблона (`template.json`)

Примеры смотрите в существующих обработчиках.

## Основные изменения

### До (с Docsvision)
- ❌ Зависимость от Docsvision
- ❌ Только задачи Docsvision
- ❌ Нет real-time
- ❌ Нет тестового приложения

### После (MVP)
- ✅ Универсальный, без зависимостей
- ✅ Любые типы уведомлений
- ✅ SignalR real-time
- ✅ Полное тестовое приложение
- ✅ Расширяемая архитектура

## Технологии

### Backend
- .NET 8 / ASP.NET Core Web API
- Entity Framework Core + SQLite
- SignalR для real-time
- Handlebars.NET для шаблонов

### Frontend
- React 18 + TypeScript
- MobX для управления состоянием
- SignalR клиент
- Tailwind CSS

## Безопасность

✅ CodeQL анализ: 0 проблем  
✅ Нет уязвимостей  
✅ Правильная настройка CORS  

## Следующие шаги

Проект готов к использованию! Вы можете:

1. Использовать существующие обработчики
2. Добавить свои типы уведомлений
3. Интегрировать в свое приложение
4. Настроить SMTP для email
5. Добавить новые каналы доставки

## Помощь

Полная документация:
- `README.md` - основная документация на английском
- `MVP_SUMMARY.md` - подробное описание MVP
- `testapp/README.md` - описание тестового приложения

## Статус

🎉 **MVP ЗАВЕРШЕН И ГОТОВ К ИСПОЛЬЗОВАНИЮ**

Все задачи из исходного запроса выполнены:
- ✅ Созданы тестовые обработчики событий уведомлений
- ✅ Создано тестовое приложение с компонентом InApp
- ✅ Удалены все зависимости от Docsvision
- ✅ Проект полностью универсальный и расширяемый
