# Example environment configuration
# Copy this file to .env and fill in your values

VITE_API_URL=https://dev-api.azerqtech.pw
VITE_SIGNALR_URL=https://dev-api.azerqtech.pw/notificationHub

# Optional: Provide access token from your auth system
# If not provided, the app will use auto-authentication (refresh token -> Windows -> Email)
VITE_ACCESS_TOKEN=

# Note: User ID is NOT needed - it's extracted from JWT token on backend
# The backend uses ClaimsPrincipal.GetApplicationUser() to get user info from the token

# 'light' | 'dark'
VITE_ICONS_THEME='light'